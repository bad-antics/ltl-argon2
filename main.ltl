// ltl-argon2/main.ltl
//
// Argon2i / Argon2id password-hashing KDF.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-argon2 v0.1.0")
    io::println("Argon2i / Argon2id password-hashing KDF.")
    0
}
